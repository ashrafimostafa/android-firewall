package com.example.vpnlearn.utility;

//this class is just for writing notes
public class Note {

    /*
        information can be find in :
            - https://developer.android.com/work/dpc/network-telephony


        test dpc source code:
            - https://github.com/googlesamples/android-testdpc
    */



    /*
        options:
           - Ensure a secure network connection for work traffic(always on vpn / lockdown / allow,disallow app )
           - Checking network usage of apps(android.app.usage.NetworkStatsManager)
           - Sets the global Private DNS host (setGlobalPrivateDnsModeSpecifiedHost)
           - Enable/Disable application
           - Enable/Disable app uninstalling
           - Network log(retrieveNetworkLogs): to retrieve the most recent batch of network logging events

     */
}
